"""Google Slides MCP — fast, typed tools for deck construction.

Wraps `gslides-api` for its persistent client + markdown writer + thumbnail
tool; adds shape-creation, transforms, fills, z-order, backgrounds, cross-deck
copy via Apps Script, and a raw-API escape hatch on top.
"""

__version__ = "0.1.0"
